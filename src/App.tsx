import { RadioInput } from "./components/data-input";

export default function App() {
  return (
    <main className="flex flex-col items-center justify-between">
      <RadioInput color="primary" inputSize="md" name="radioGroup1" />
    </main>
  );
}
